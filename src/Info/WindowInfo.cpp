/*****************************************************************//**
 * @file   WindowInfo.cpp
 * @brief  WindowInfoç\ë¢ëÃÇÃé¿ëï
 * 
 * @author ichi-raven
 * @date   November 2022
 *********************************************************************/
#include "../../include/Info/WindowInfo.hpp"

namespace Hence
{
    //WindowInfo::WindowInfo() noexcept
    //{}

    //WindowInfo::WindowInfo(const std::uint32_t width, const std::uint32_t height, const std::uint32_t frameCount, const std::string_view windowName, bool fullScreen, bool vsync) noexcept
    //    : width(width), height(height), frameCount(frameCount), windowName(windowName), vsync(vsync), fullScreen(fullScreen)
    //{}

    //WindowInfo::WindowInfo(const std::uint32_t width, const std::uint32_t height, const std::uint32_t frameCount, const char* windowName, bool fullScreen, bool vsync) noexcept
    //    : width(width), height(height), frameCount(frameCount), windowName(windowName), vsync(vsync), fullScreen(fullScreen)
    //{}
}
